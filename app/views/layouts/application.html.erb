<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || t(".title") %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100 text-gray-800">
    <header class="bg-white shadow-md">
      <nav class="container mx-auto flex justify-between items-center p-4">
        <div>
          <%= link_to t(".title"), root_path, class: "text-xl font-bold", data: { testid: "home-link" } %>
          <%= link_to t(".courses"), courses_path, class: "ml-4 text-blue-600 hover:text-blue-800", data: { testid: "courses-link" } %>
          <%= link_to t(".syllabus"), "/syllabuses", class: "ml-4 text-blue-600 hover:text-blue-800", data: { testid: "syllabus-link" } %>
        </div>
        <div>
          <% if current_user %>
            <span class="mr-4"><%= t(".logged_in_as", name: current_user.name) %></span>
            <%= button_to t(".log_out"), logout_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded", data: { testid: "logout-button" } %>
          <% else %>
            <%= link_to t(".sign_up"), signup_path, class: "mr-4 bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded", data: { testid: "signup-link" } %>
            <%= link_to t(".log_in"), login_path, class: "bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded", data: { testid: "login-link" } %>
          <% end %>
        </div>
      </nav>
    </header>
    <div class="container mx-auto px-4 mt-4">
      <% flash.each do |type, msg| %>
        <div class="<%= flash_class(type) %> p-3 rounded-md mb-4" data-testid="flash-message">
          <%= msg %>
        </div>
      <% end %>
    </div>
    <main class="container mx-auto p-4">
      <%= yield %>
    </main>
  </body>
</html>
